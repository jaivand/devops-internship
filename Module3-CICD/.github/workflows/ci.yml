name: CI Check

on:
  push:
    branches: [ "main" ]

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Make script executable
        run: chmod +x web-hosting-week2/system-info.sh

      - name: Run system info script
        run: ./web-hosting-week2/system-info.sh

      - name: Final Message
        run: echo "Week 3 CI Successful"
